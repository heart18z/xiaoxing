# 小醒交互优化与网页验收发布

## 本次调整

- 注册/新密码输入增加可访问的显示与隐藏按钮，44px点击区域，重新打开表单恢复遮蔽，不改变密码内容。
- 录音确认先暂停PCM采集并关闭本组件持有的麦克风轨道，再编码和请求转写。录音波形显示真实音量历史；转写阶段改为独立进度圈与“麦克风已关闭，无需继续说话”，8秒后提示服务响应较慢。取消与失败保留原草稿，不自动发送。
- 原生iOS Speech桥接增加约80ms一次的RMS音量事件，并在结束采音时停用音频会话。Windows不能编译或验证Swift真机行为，需Mac构建后验收。
- 思考折叠时去掉标题下边框，保留正文上方的一条分隔线。
- 页面切换取消先淡出再进入，保留背景与轻量内容过渡。加载遮罩改为延迟350ms出现的3px非阻挡进度条。
- 三个Tab缓存已加载内容，返回时静默刷新；底部增加按压/选中反馈并遵循减少动态效果设置。离开前记录列表滚动位置，返回后恢复。退出登录清空缓存，避免跨账号草稿和消息残留。

## 验证

- 使用 [Playwright技能](C:/Users/123/.codex/skills/playwright/SKILL.md) 的CLI执行隔离浏览器检查，API、账号与原生桥接均使用测试替身；没有操作真实用户数据。
- 前端Node测试12项通过，网页生产包、原生网页包构建成功。保留既有大包/eval构建警告；未声称完成Xcode归档。
- 账号/资料/通知/语音/键盘浏览器回归11组通过；麦克风测试使用合成音频流，确认转写等待期间轨道已结束、波形消失，静音不上传、错误仅一次显示。
- 页面交互回归4组通过：思考单分隔线、Tab按下缩放约0.93、3px加载条不挡点击、草稿/筛选/600px列表位置恢复、激活刷新、快速切换52个采样帧无空白、退出再以另一模拟账号登录无旧草稿/消息。
- 公网HTML与本地SHA256一致，4个入口JS/CSS返回200；公网注册眼睛操作正常（未提交注册）。账号接口拒绝与预检8项、既有公网CORS18项通过。
- 视觉截图位于 `output/playwright/interaction-*.png`，已检查录音、转写、单分隔线及慢加载布局。

## 发布和回滚

- 已发布：https://www.chentong.xyz/app/login 。网页刷新生效；iOS本地资源必须重新打包上传TestFlight后更新安装。
- 网页目录：`/opt/aimessage/releases/app-interaction-20260914-1605/web/dist`。
- 备份记录：`/opt/aimessage/backups/pre-app-interaction-20260914-1605/release.json`。
- 前版目录保留：`/opt/aimessage/releases/profile-polish-20260914-1530/web/dist`。保留旧版哈希资源，避免旧浏览器页面的懒加载资源失效。
- 包SHA256：`063c91ffb4dc00c221b663061e5c03e89e5a915bbccfa9f113dd1e15b04b1f82`。
- HTML SHA256：`2637c342614fa46d58733f93d9f5904f4783dad66a7013517d8f1ede631f4691`。
- 发布只切换网页软链接，未改后端镜像、数据库、配置或密钥。发布前后后端容器ID/镜像/启动时间/重启次数一致，最终healthy、重启次数0。
- 如确需回滚，确认没有后续部署后在服务器执行 `python3 /opt/aimessage/deploy-app-interaction-20260914.py rollback`。脚本只允许当前指向本次版本时恢复前版网页；不回滚数据库或语音通道。
- 本次没有Git提交/推送或TestFlight发布。
