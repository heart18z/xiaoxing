# 三项界面微调

- 底部 Tab：选中底色收窄为 76px 柔和渐变，增加浅色细边框和短指示线，保留切换位移、按下反馈及减少动画偏好。
- 好友列表：分为“权限设定”“编辑资料”两个独立按钮，右侧上下排列。权限直接进入原权限申请面板；编辑资料查看好友基本信息、修改当前用户自己的备注，不修改好友本名或联系方式。
- 提醒侧栏：日期按钮增加统一日历底标、展开箭头和聚焦效果，还原使用线性 SVG 图标；筛选分类按钮从 42px 缩至 32px，缩窄宽度。日期校验、还原、列表滚动逻辑保持不变。

验证：33 项 Node 回归通过；Playwright 使用模拟账号/接口，检查两个入口、备注保存和清空不改变本名、320px 窄屏、日期选择与还原、小尺寸 Tab，并回归原好友查找/闹铃/固定高度对话框。页面截图已检查。测试未使用真实用户数据。

发布：仅静态前端 `/opt/aimessage/releases/ui-three-20260915/web/dist`，保留旧散列资源并原子切换 current；原版本记录 `/opt/aimessage/backups/pre-ui-three-20260915/state.json`。不改数据库、不重启后端、不调整证书配置。后端继续使用 `aimessage-backend:drawer-ip-20260915`。

发布包 SHA256：`eabaef2316526026cf51c36479f6d497a4fa7ac0d1c8e1fc9b333fd9ceabdc71`。

已完成发布。切换后首次检查发现新 release 根目录受 umask 影响为 0700，导致 Nginx 静态入口短暂 500；检查确切目录后修复为 0755，并修正发布脚本。再次公网 curl 和浏览器访问均正常，页面文件 SHA256 与本地构建一致：`71f1906b049f8eaaff42ec88fcaabc3a1abbcbb4788ac4bdfc1867e6575239aa`。后端始终 healthy，未重启、未调整接口权限。

`build:prod`、`build:native`、`cap copy ios` 均通过。iOS 本地资源已同步，但没有生成签名安装包；旧 App 需重新打包安装后才能获得本次界面变化。没有提交 Git。
